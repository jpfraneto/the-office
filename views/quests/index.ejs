<%- include("../partials/header") %>
<div class="container">
        <h1>Quests</h1>
        <p>The following is the list of quests/missions/tasks that I have to fulfill to move towards a better/more functional platform:</p>
        <table class="table table-dark">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Date</th>
                <th scope="col">Name</th>
                <th scope="col">More:</th>
              </tr>
            </thead>
            <tbody>
                <% for (i=0; i<quests.length; i++){ %>
                    <tr>
                        <th scope="row"><% i %></th>
                        <td><%= quests[i].date %></td>
                        <td><%= quests[i].name %></a></td>
                        <td><a href="/quests/<%=quests[i]._id%>">More</a></td>
                    </tr>
                <% } %>
            </tbody>
          </table>

<div class="text-center">
        <a class="btn btn-primary" href="/">Back to <strong>The Office</strong></a>
        <a class="btn btn-success" href="/quests/new">Add new Quest</a>
</div>
</div>
<%- include("../partials/footer") %>